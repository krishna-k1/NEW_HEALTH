<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hospital Billing</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="styles/bootstrap/dist/css/bootstrap.min.css" />
   
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Nerko+One&family=Rubik:ital,wght@0,300..900;1,300..900&display=swap" rel="stylesheet">
    <style>
        body {
            background: radial-gradient(circle, #d0f7d4, #a8e6e0);
            font-family: Arial, sans-serif;
        }

        h1, h2 {
            text-align: center;
            color: #073809;
        }

        form {
            max-width: 800px;
            margin: 0 auto;
            background: white;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        label {
            font-weight: bold;
            display: block;
            margin-top: 15px;
        }

        input, select, button {
            width: 100%;
            padding: 10px;
            margin-top: 5px;
            margin-bottom: 10px;
            border: 1px solid #ddd;
            border-radius: 5px;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 15px;
        }

        th, td {
            border: 1px solid #ddd;
            padding: 10px;
            text-align: left;
        }

        th {
            background-color: #073809;
            color: white;
        }

        button {
            background-color: #073809;
            color: white;
            cursor: pointer;
        }

        button:hover {
            background-color: #065d08;
        }

        #summary {
            margin-top: 20px;
        }

        .total {
            font-size: 20px;
            font-weight: bold;
            text-align: right;
            color: #073809;
        }

        .bill-summary {
            width: 100%;
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
            background-color: white;
            border: 1px solid #ddd;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        .bill-summary h3 {
            text-align: center;
            margin-bottom: 20px;
        }

        .bill-summary table {
            width: 100%;
            margin-bottom: 20px;
        }

        .bill-summary th, .bill-summary td {
            text-align: left;
            padding: 8px;
            border: 1px solid #ddd;
        }

        .bill-summary th {
            background-color: #073809;
            color: white;
        }

        .bill-summary td {
            background-color: #f9f9f9;
        }

        .bill-summary .total {
            font-size: 22px;
            font-weight: bold;
            text-align: right;
            color: #073809;
            border-top: 2px solid #073809;
            padding-top: 10px;
        }

        .signature {
            margin-top: 30px;
            text-align: center;
            font-style: italic;
        }

        #printButton {
            width: auto;
            padding: 8px 16px;
            margin: 0 auto;
            display: block;
        }

        .contact-info {
            margin-top: 20px;
            font-size: 14px;
            text-align: center;
        }

        @media print {
            body {
                background: white;
            }
            #billingForm, #summary, h1, h2 {
                display: none;
            }
            .bill-summary {
                width: 100%;
                padding: 10px;
            }
            .bill-summary table, .bill-summary th, .bill-summary td {
                border: 1px solid #000;
            }
            .bill-summary h1 {
                font-size: 30px;
                margin-bottom: 20px;
            }
        }
        .navbar-collapse {
    transition: background 0.3s ease; /* Smooth transition for the entire background property */
}

.navbar-collapse.show {
    background: radial-gradient(circle, rgba(208, 247, 212, 0.5), rgba(168, 230, 224, 0.7));
}


.navbar {
  --bs-navbar-padding-y: 0px;
  width: 100%;
  position: fixed;
  top: 0;
  left: 0;
  z-index: 1000;


}

.profile-summary {
    border-top: 1px solid #ccc;
    padding-top: 10px;
}

.profile-summary h3 {
    margin-bottom: 10px;
}

.profile-summary p {
    font-size: 16px;
    margin: 5px 0;
}

.profile-summary strong {
    font-weight: bold;
}


/* Adjust navbar height on smaller screens */
@media (max-width: 768px) {
 .navbar{
  height: 50px;}

  .navbar-brand {
    font-size: 1rem; /* Optionally reduce logo font size */
  }

  .nav-link {
    padding: 5px 10px; /* Reduce padding of nav links */
  }

  .navbar-toggler {
    padding: 3px 6px; /* Reduce padding of toggle button */
  }
}


.nav-item {
  padding-left: 20px;
}

/* Underline effect on hover */
.nav-link {
  position: relative;
  color: #333; /* Optional: Adjust text color */
  text-decoration: none; /* Remove default underline */
}

.nav-link::after {
  content: "";
  position: absolute;
  width: 0;
  height: 2px;
  bottom: -2px;
  left: 0;
  background-color: #3f8b42; /* Change this color as desired */
  transition: width 0.3s ease-in-out;
}

.nav-link:hover::after {
  width: 100%; /* Expands underline on hover */
}
/* Adjust navbar height on smaller screens */
@media (max-width: 768px) {
 .navbar{
  height: 50px;}

  .navbar-brand {
    font-size: 1rem; /* Optionally reduce logo font size */
  }

  .nav-link {
    padding: 5px 10px; /* Reduce padding of nav links */
  }

  .navbar-toggler {
    padding: 3px 6px; /* Reduce padding of toggle button */
  }
}
.navbar .btn {
    padding: 10px 15px;
    font-size: 1rem;
    width: auto;
}

.navbar-toggler {
    width: auto; /* Ensures the button only occupies its content width */
    max-width: 50px; /* Optional: Restrict maximum width */
}


.container-fluid {
background: linear-gradient(to right, #d3f6f9, #a1e4a6);
background-size: cover;
height: 15vh;
}

.container-wrapper, .about-container, .footer-container {
width: 80%; /* Consistent width for all containers */
  max-width: 1200px; /* Optional: limit width for larger screens */
margin: 20px auto; /* Center containers and add spacing */
padding: 20px;
  border-radius: 8px;
box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
}

.fullpage{
    padding-top: 80px;
}
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg bg-body-tertiary">
        <div class="container-fluid">
            <a class="navbar-brand" href="javascript:void(0)" onclick="scrollToTop()">
                <img src="/assets/images/logo copy.png" alt="logo" width="30px" height="30px"> Health-Ease
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation" id="navbar-toggle-btn">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page" href="/start">Home</a>
                    </li>
                 
                    <!-- <li class="nav-item">
                        <a class="nav-link active" href="/views/heartDiagnose">DiseaseDiagnosis</a>
                    </li> -->
<!--     
                        <li class="nav-item">
                      <a class="nav-link active" href="/views/appointment">Schedule appointment</a>
                  </li>
                  <li class="nav-item">
                    <a class="nav-link active" href="/views/heartDiagnose">Heart Diagnose</a>
                </li>
                    -->
                
                </ul>
                <button class="btn btn-outline-success" type="submit">
                    <a class="nav-link" href="/logout">LogOut</a>
                </button>
            </div>
        </div>
    </nav>

    <div class="fullpage">

    <h1>HopeHeart Medical Clinic</h1>
    <h2>Billing Page</h2>
    <form id="billingForm">
        <label for="patientName">Patient Name</label>
        <input type="text" id="patientName" placeholder="Enter patient's name" required>

        <label for="doctorName">Doctor's Name</label>
        <input type="text" id="doctorName" placeholder="Enter doctor's name" required>

        <label for="hoursStayed">Number of Hours Stayed</label>
        <input type="number" id="hoursStayed" placeholder="Enter hours stayed" required>

        <label for="roomType">Room Type</label>
        <select id="roomType" required>
            <option value="general">General Ward ($50/hour)</option>
            <option value="semi-private">Semi-Private ($100/hour)</option>
            <option value="private">Private ($200/hour)</option>
        </select>

        <label for="prescriptions">Medications and Prescriptions</label>
        <table id="prescriptionTable">
            <thead>
                <tr>
                    <th>Medicine Name</th>
                    <th>Cost ($)</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td><input type="text" placeholder="Medicine name"></td>
                    <td><input type="number" placeholder="Cost" step="0.01"></td>
                    <td><button type="button" class="removeRow">Remove</button></td>
                </tr>
            </tbody>
        </table>
        <button type="button" id="addPrescription">Add Medicine</button>

        <button type="button" id="generateBill">Generate Bill</button>
    </form>

    <div id="summary">
        <h2>Bill Summary</h2>
        <div id="summaryContent"></div>
        <button id="printButton">Print Bill</button>
    </div>
</div>

    <script>
        const rates = {
            "general": 50,
            "semi-private": 100,
            "private": 200
        };

        const addPrescriptionBtn = document.getElementById('addPrescription');
        const prescriptionTable = document.getElementById('prescriptionTable').getElementsByTagName('tbody')[0];

        // Add a new row to the prescriptions table
        addPrescriptionBtn.addEventListener('click', () => {
            const row = prescriptionTable.insertRow();
            row.innerHTML = `
                <td><input type="text" placeholder="Medicine name"></td>
                <td><input type="number" placeholder="Cost" step="0.01"></td>
                <td><button type="button" class="removeRow">Remove</button></td>
            `;
        });

        // Remove a prescription row
        prescriptionTable.addEventListener('click', (event) => {
            if (event.target.classList.contains('removeRow')) {
                const row = event.target.closest('tr');
                row.remove();
            }
        });

        // Generate bill
        document.getElementById('generateBill').addEventListener('click', async () => {
            const patientName = document.getElementById('patientName').value;
            const doctorName = document.getElementById('doctorName').value;
            const hoursStayed = +document.getElementById('hoursStayed').value || 0;
            const roomType = document.getElementById('roomType').value;

            // Calculate room cost
            const roomCost = hoursStayed * rates[roomType];

            // Collect all medication details
            const medications = [];
            const rows_m = prescriptionTable.getElementsByTagName('tr');
            Array.from(rows_m).forEach(row => {
                const medicineInput = row.cells[0].getElementsByTagName('input')[0];
                const medicine = medicineInput ? medicineInput.value.trim() : '';
                if (medicine) medications.push(medicine);
            });
            const medicationCSV = medications.join(',');

            // Calculate medication cost
            let medicationCost = 0;
            const rows = prescriptionTable.getElementsByTagName('tr');
            Array.from(rows).forEach(row => {
                const costInput = row.cells[1].getElementsByTagName('input')[0];
                const cost = parseFloat(costInput.value);
                if (!isNaN(cost)) medicationCost += cost;
            });

            const totalAmount = roomCost + medicationCost;

             // Send data to the backend
            const response = await fetch('/save-bill', {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({ hoursStayed, roomType, medication: medicationCSV, totalAmount }),
            });

            if (response.ok) {
                alert('Billing information saved successfully!');
            } else {
                alert('Failed to save billing information.');
            }

            // Update bill summary
            const summaryContent = `
                <div class="bill-summary">
                    <h1>Bill</h1>
                    <h3>HopeHeart Medical Clinic</h3>
                    <p style="text-align: center;">123 Hospital St, Health City, HC 12345</p>
                    <p><strong>Patient Name:</strong> ${patientName}</p>
                    <p><strong>Doctor's Name:</strong> ${doctorName}</p>
                    <p><strong>Hours Stayed:</strong> ${hoursStayed} hours</p>
                    <p><strong>Room Type:</strong> ${roomType} ($${rates[roomType]}/hour)</p>
                    <p><strong>Room Charges:</strong> $${roomCost.toFixed(2)}</p>
                    <p><strong>Medications Cost:</strong> $${medicationCost.toFixed(2)}</p>
                    <p><strong>Date & Time:</strong> ${new Date().toLocaleString()}</p>
                    <p class="total"><strong>Total Amount:</strong> $${totalAmount.toFixed(2)}</p>
                    <div class="signature">
                        <p>Authorized Signature</p>
                    </div>
                    <div class="contact-info">
                        <p>Email: info@xyzhospital.com | Contact: (123) 456-7890</p>
                    </div>
                </div>
            `;
            document.getElementById('summaryContent').innerHTML = summaryContent;
            document.getElementById('summary').style.display = 'block';
        });

        // Print bill
        document.getElementById('printButton').addEventListener('click', () => {
            window.print();
        });
    </script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>

</body>
</html>
